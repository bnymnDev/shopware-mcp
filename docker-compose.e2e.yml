# Local Shopware for end-to-end tests: `docker compose -f docker-compose.e2e.yml up -d --wait`
services:
  shopware:
    # DOCKWARE_TAG picks the Shopware version, e.g. 6.6.10.6; latest is the newest 6.7.
    image: dockware/dev:${DOCKWARE_TAG:-latest}
    ports:
      - "8000:80"
    environment:
      SW_API_ACCESS: "true"
      XDEBUG_ENABLED: "0"
    healthcheck:
      # /api/_info/version needs a token and answers 401; the health-check route is public.
      test: ["CMD", "curl", "-fsS", "http://localhost/api/_info/health-check"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 60s
